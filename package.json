{
  "private": true,
  "scripts": {
    "build": "npm run generate:radix-scss && quarto render",
    "deploy:cloudflare": "scripts/deploy-cloudflare-pages.sh",
    "generate:radix-scss": "node scripts/generate-radix-scss.mjs"
  },
  "devDependencies": {
    "@radix-ui/colors": "^3.0.0"
  }
}
